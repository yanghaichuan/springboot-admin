<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yhc.example.employ.mapper.SysEmployMapper">

<select id="selectSysEmployCount" resultType="com.yhc.example.employ.domain.SysEmployCount">
    SELECT
        DATE_FORMAT( create_time, '%Y-%m-%d' ) as employTime,
        count(1) as employNum
    FROM
        sys_employ
    GROUP BY
        DATE_FORMAT(
        create_time,
        '%Y-%m-%d')
    ORDER BY DATE_FORMAT( create_time, '%Y-%m-%d' ) desc LIMIT 10
</select>
</mapper>
